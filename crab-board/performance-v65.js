(()=>{if(window.__crabBoardPerformance65)return;window.__crabBoardPerformance65=true;let limit65=10,key65='';renderFeed=function(){const key=selectedYear+'|'+active;if(key!==key65){key65=key;limit65=10}const all=posts(),list=all.slice(0,limit65);$('feed').innerHTML=list.length?list.map(r=>{const m=metadata(r),comments=r.comments||[];return `<article class="post card" data-id="${esc(r.id)}"><div class="postHead"><div class="avatar">${esc(initial(m.partner))}</div><div class="postMeta"><div class="partner">${esc(m.partner||'取引先')}</div><div class="time">投稿日：${esc(formatDate(m.createdAt,r.date))}</div></div><button class="deleteButton" type="button" data-action="delete">削除</button></div>${m.text?`<div class="postText">${esc(m.text)}</div>`:''}${r.sheet_url?`<img class="postImage" loading="lazy" decoding="async" src="${r.sheet_url}" alt="${esc(m.partner||'取引先')}の投稿画像">`:''}<div class="commentArea"><div class="commentList">${comments.length?comments.map(c=>`<div class="comment">${esc(typeof c==='string'?c:c.text||'')}</div>`).join(''):'<div class="status">コメントはありません。</div>'}</div><div class="commentComposer"><input class="commentInput" placeholder="コメントを入力"><button class="commentButton" type="button" data-action="comment">コメント</button></div></div></article>`}).join(''):`<div class="empty">${selectedYear}年の${active==='すべて'?'投稿':active+'の投稿'}はまだありません。</div>`;if(all.length>list.length)$('feed').insertAdjacentHTML('beforeend',`<button id="morePosts65" class="primary" type="button">さらに表示（残り${all.length-list.length}件）</button>`);bindFeed();const more=$('morePosts65');if(more)more.onclick=()=>{limit65+=10;renderFeed()}};compressImage=function(file){return new Promise((resolve,reject)=>{const rd=new FileReader();rd.onerror=reject;rd.onload=()=>{const img=new Image();img.onerror=reject;img.onload=()=>{const max=1000,scale=Math.min(1,max/Math.max(img.width,img.height)),w=Math.round(img.width*scale),h=Math.round(img.height*scale),canvas=document.createElement('canvas');canvas.width=w;canvas.height=h;canvas.getContext('2d').drawImage(img,0,0,w,h);resolve(canvas.toDataURL('image/jpeg',.72))};img.src=rd.result};rd.readAsDataURL(file)})}})();